*Alignment по предпочтениям сдвигает вероятности ответов в сторону выбранных людьми и одновременно ограничивает уход от reference-модели. PPO, DPO и GRPO различаются вычислительной механикой, но наследуют одну уязвимость: оптимизируют измеренный proxy.*

## От демонстрации к сравнению

Типичный pipeline начинается с pretrained language model. Затем:

1. supervised fine-tuning, SFT, на хороших ответах;
2. сбор попарных предпочтений;
3. обучение [reward model](/lesson/80) или прямое использование пар;
4. оптимизация policy;
5. независимая человеческая и автоматическая оценка.

SFT учит имитировать демонстрации. Preference optimization отвечает на более тонкий вопрос: среди нескольких правдоподобных продолжений какие предпочтительнее. Она не заменяет знания из [предобучения](/lesson/78), а меняет распределение ответов.

Обозначим prompt $x$, ответ $y$, policy $\pi_\theta(y\mid x)$ и замороженную reference $\pi_{\mathrm{ref}}$.

:::sidenote{title="Контроль: где появляется поведение?" label="Вопрос"}
Если preference dataset содержит только задачи на краткость, научится ли модель безопасно отвечать на опасные запросы? Оптимизатор не создаёт критерий, которого нет в сигнале.
:::

:::figure{src="/figures/lessons/81/figure-1.svg" id="fig-81-1" title="Пять контуров alignment pipeline" alt="Поток pretrained model SFT preferences reward optimization evaluation"}
Стрелки различают данные и параметры. Замороженные блоки показаны контуром: reference и evaluation set не обновляются. Красная обратная стрелка от production-аудита к сбору данных показывает итеративность, но test-примеры не попадают в train.
:::

## Почему нужен reference

Если максимизировать только learned reward $r_\phi(x,y)$, policy найдёт области, где reward model ошибается. Добавляют штраф Кульбака–Лейблера:

$$
\max_\pi\;
\mathbb E_{y\sim\pi(\cdot\mid x)}
\left[
r_\phi(x,y)
-\beta\log\frac{\pi(y\mid x)}
{\pi_{\mathrm{ref}}(y\mid x)}
\right].
$$

В среднем второе слагаемое равно $-\beta D_{\mathrm{KL}}(\pi\|\pi_{\mathrm{ref}})$. Большая $\beta$ удерживает привычное поведение, малая разрешает сильное изменение.

Для фиксированного reward оптимальная policy имеет форму

$$
\pi^*(y\mid x)\propto
\pi_{\mathrm{ref}}(y\mid x)\exp(r(x,y)/\beta).
$$

Reference задаёт prior: ответ с почти нулевой исходной вероятностью трудно поднять, если reward конечен.

:::sidenote{title="Контроль: температура обновления" label="Вопрос"}
Два ответа имеют одинаковую reference-вероятность, rewards отличаются на 2. Во сколько раз отличаются их веса в $\pi^*$ при $\beta=1$ и $\beta=0{,}5$?
:::

## PPO: осторожный шаг по sampled ответам

PPO генерирует ответы текущей policy, получает rewards и оценивает advantage $\widehat A_t$. Probability ratio:

$$
\rho_t(\theta)=
\frac{\pi_\theta(a_t\mid s_t)}
{\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)}.
$$

Clipped objective:

$$
\mathcal L_{\mathrm{clip}}
=\mathbb E\min\left[
\rho_t\widehat A_t,\;
\operatorname{clip}(\rho_t,1-\varepsilon,1+\varepsilon)
\widehat A_t
\right].
$$

Если обновление слишком увеличивает вероятность выгодного действия, clipping перестаёт поощрять дальнейший рост в этом batch. Critic оценивает value, а KL к reference добавляется в reward или loss.

PPO on-policy: после заметного обновления старые samples устаревают. Pipeline дорогой — policy, reference, reward model и critic участвуют одновременно.

:::definition{title="Policy update"}
Policy update меняет вероятности токенов, а через их произведение — вероятности целых ответов. Даже маленькие изменения на каждом шаге могут дать большой сдвиг для длинной последовательности.
:::

## DPO: пары сразу задают логистическую цель

DPO использует аналитическую связь reward с отношением policy/reference. Для preferred $y^+$ и rejected $y^-$:

$$
\mathcal L_{\mathrm{DPO}}=
-\log\sigma\left(
\beta
\left[
\log\frac{\pi_\theta(y^+\mid x)}
{\pi_{\mathrm{ref}}(y^+\mid x)}
-
\log\frac{\pi_\theta(y^-\mid x)}
{\pi_{\mathrm{ref}}(y^-\mid x)}
\right]
\right).
$$

Отдельный reward model и online sampling не нужны: обучение похоже на supervised pass по парам. Но данные фиксированы. DPO не увидит новые странные ответы, которые policy начнёт порождать после сдвига, пока их не соберут в следующем цикле.

Пара предпочтений говорит только относительное. Если оба ответа плохи, DPO всё равно усиливает меньший из двух зол. Нужны абсолютные фильтры и качественный сбор кандидатов.

:::figure{src="/figures/lessons/81/figure-2.svg" id="fig-81-2" title="PPO и DPO используют один сигнал разными маршрутами" alt="Параллельные схемы PPO online sampling reward critic и DPO offline preference pairs"}
Слева PPO замыкает online-цикл генерации, scoring и обновления. Справа DPO получает замороженные пары и reference log-probabilities. Общая нижняя ось показывает сдвиг log-odds chosen против rejected и ограничение reference.
:::

## GRPO: сравнение внутри группы

Group Relative Policy Optimization генерирует группу ответов $y_1,\ldots,y_G$ на один prompt и нормирует rewards внутри группы:

$$
\widehat A_i=
\frac{r_i-\overline r}
{s_r+\varepsilon}.
$$

Отдельный value model может не требоваться. Сравнение внутри одного prompt убирает часть различий сложности между prompt. Затем policy обновляется PPO-подобной clipped целью с KL.

Если все rewards группы почти равны, деление на малое $s_r$ нестабильно; $\varepsilon$ и фильтрация групп существенны. Если reward model систематически предпочитает длину, относительная нормировка не убирает bias.

:::sidenote{title="Контроль: группа одинаковых ответов" label="Вопрос"}
Все четыре ответа получили reward 1,00. Какой полезный direction даст group-relative advantage? Что это говорит о разнообразии sampling?
:::

## Лаборатория alignment

:::widget{name="g11-generative" lesson="81" title="Reward, KL и смещение вероятностей ответов"}
:::

Выберите два ответа с заданными reference probabilities и rewards. Меняйте $\beta$: при малом значении policy быстро концентрируется на лидере, при большом остаётся близкой к reference. Добавьте reward за длину и наблюдайте, как оптимизация наращивает токены.

Сравните offline-пары DPO с online-циклом. После сдвига policy старый rejected может исчезнуть, а появится новый exploit. Только новый сбор сравнений обнаружит его.

## Длина как лазейка

Reward часто коррелирует с длиной: подробные ответы действительно полезнее в train. Policy учится добавлять вступления и повторы. Диагностика:

- reward против числа токенов;
- human preference на length-matched парах;
- regress reward на длину и анализ residual;
- отдельный penalty или явный критерий краткости;
- тест prompts, где правильный ответ должен быть коротким.

Нельзя просто запретить длинные ответы: сложная задача требует пространства. Цель должна учитывать prompt и полноту.

:::figure{src="/figures/lessons/81/figure-3.svg" id="fig-81-3" title="Reward растёт после того, как польза остановилась" alt="Кривые reward human score длины и KL по шагам preference optimization"}
По горизонтали шаги оптимизации. Learned reward продолжает расти, human score выходит на плато, средняя длина и KL ускоряются. Вертикальная линия отмечает checkpoint, выбранный по независимой оценке, а не последний.
:::

## Как сравнивать методы

Одинаковая preference accuracy не означает одинаковый aligned model. Сравнение PPO, DPO и GRPO требует:

- одной стартовой SFT и reference;
- сопоставимого preference signal;
- одинакового generation/evaluation protocol;
- нескольких seed;
- win rate с доверительным интервалом;
- KL и длины;
- factuality, safety, helpfulness по отдельности;
- проверок на distribution shift.

LLM-as-a-judge масштабирует оценку, но наследует bias судьи: порядок, стиль, самопредпочтение. Часть выборки должна быть оценена людьми вслепую.

:::sidenote{title="Победа 52% может быть ничьей" label="Проверка"}
Без числа пар и парного дизайна процент неинформативен. Один и тот же prompt надо показывать обоим моделям, порядок рандомизировать, а интервал считать по prompt.
:::

### Мини-исследование: Pareto-кривая KL и предпочтений

Обучите несколько checkpoint при $\beta\in\{0{,}03;0{,}1;0{,}3;1\}$ или остановите один запуск на разных шагах. Для каждого измерьте held-out win rate, KL к reference, длину, diversity и factual error rate. Нанесите точки на плоскость win rate–KL.

Точки, для которых существует другая модель одновременно с большим win rate и меньшим KL, доминируемы. Остальные образуют Pareto frontier. Выбор на frontier требует предметной цены ухода от reference; максимальный win rate не является автоматическим решением.

Добавьте prompts вне preference-домена. Если KL мал в среднем, он всё же может быть велик на узкой группе. Покажите распределение per-prompt KL и верхний процентиль. Это тот же урок, что в [оценке по срезам](/lesson/44): среднее скрывает локальное изменение поведения.

### Проверка reward hacking до дорогого запуска

Постройте набор controlled pairs: одинаковое содержание, но разная длина, число заголовков, уверенность тона и повтор ключевых слов. Прогоните reward model до policy optimization. Если surface feature систематически повышает score, у вас есть прогнозируемая лазейка.

Затем включите adversarial generation: попросите простую policy или поиск максимизировать reward при сохранённом prompt. Каждые 20 шагов отдавайте samples независимым людям вслепую. График proxy–human divergence определяет stop point. Это практическое продолжение [Goodhart-эффекта reward model](/lesson/80), а не постфактум объяснение.

Проверьте reversibility. После preference optimization выполните небольшой SFT на исходных demonstrations и измерьте, возвращаются ли длина, KL и качества. Если поведение не возвращается, обновление затронуло представления шире наблюдаемого preference direction.

Сохраните checkpoints и evaluation traces, но не выбирайте лучший по каждому benchmark отдельно. Один заранее заданный rule выбора защищает от скрытого ансамбля из разных удачных эпох.

Для judge-модели переставляйте ответы местами: пары, где решение меняется, требуют человеческой проверки, а не молчаливого усреднения.

## Alignment не заканчивается loss

После обучения нужны system-level меры: retrieval, tool permissions, отказоустойчивый интерфейс, logging, red teaming. Модель может правильно отказаться в тесте и ошибиться в длинном агентном цикле. [Агентные системы](/lesson/89) умножают вероятности ошибок по шагам.

Преференции также меняются между группами и контекстами. Полезная система позволяет уточнить цель, показывает неопределённость и не выдаёт один усреднённый вкус за универсальный.

## Proxy требует независимого суда

PPO использует online trajectories и critic, DPO учится прямо на offline-парах, GRPO строит относительный advantage внутри группы. KL удерживает обновление возле reference, но не гарантирует истину. Главная проверка — разрыв между learned reward и независимым человеческим результатом при сильной оптимизации.

## Задачи

:::problem{points="3"}
Для двух единственно возможных ответов заданы ненормированные reference-веса $0{,}2$ и $0{,}1$ и rewards 3 и 1. Сначала нормируйте reference-веса до распределения, затем по формуле $\pi^*(i)=\pi_{\mathrm{ref}}(i)e^{r_i/\beta}/\sum_j\pi_{\mathrm{ref}}(j)e^{r_j/\beta}$ вычислите обе вероятности при $\beta=1$ и $\beta=0{,}5$. Проверьте сумму вероятностей и объясните, почему уменьшение $\beta$ усиливает различие.
:::

:::problem{points="4"}
Для одного PPO-сэмпла $\widehat A=2$, old probability 0,20, new probability 0,27, $\varepsilon=0{,}2$. Вычислите ratio, unclipped и clipped terms и contribution objective. Повторите для $\widehat A=-2$.
:::

:::problem{points="5"}
Для DPO-пары loss равен
$-\log\sigma\!\left(\beta[(\log\pi(y^+)-\log\pi(y^-))-(\log\pi_{\rm ref}(y^+)-\log\pi_{\rm ref}(y^-))]\right)$.
Log-probabilities policy равны $(-4,-5)$ для chosen/rejected, reference —
$(-3{,}5,-4)$, $\beta=0{,}5$. Вычислите argument sigmoid и loss. Как
изменится loss, если обе policy log-probabilities уменьшить на одну и ту же
константу?
:::

:::problem{points="6"}
С seed 2026 сгенерируйте 200 независимых групп по четыре ответа. Для ответа
$i$ задайте истинную полезность $u_i\sim\mathcal N(0,1)$, длину
$\ell_i\sim\mathrm{Uniform}\{40,\ldots,200\}$ независимо от $u_i$ и proxy
$r_i=u_i+0{,}03(\ell_i-120)+\varepsilon_i$, где
$\varepsilon_i\sim\mathcal N(0,0{,}2^2)$. В каждой группе заведите три копии
logits, начните с нулей и 100 раз выполните точный policy-gradient update
$z_i\leftarrow z_i+0{,}1\pi_i(A_i-\sum_j\pi_jA_j)$,
$\pi=\mathrm{softmax}(z)$. В первом режиме используйте $A_i=r_i$; во втором
— $(r_i-\bar r_g)/(s_g+10^{-8})$, где
$s_g=\sqrt{\frac14\sum_{i=1}^4(r_i-\bar r_g)^2}$; в третьем сначала оцените
на всех 800 ответах OLS-регрессию $r=a+b\ell$, затем используйте
$A_i=r_i-\widehat b(\ell_i-120)$. После каждого шага усредните по политикам
ожидаемые истинную полезность, длину и proxy reward. Сравните три кривые и
сформулируйте, какой режим эксплуатирует длину, а какой подавляет этот эффект.
:::

:::problem{points="7"}
Используйте `Anthropic/hh-rlhf` revision
`09be8c5bbc57cb3887f3a9732ad6aa7ec602a1fa`, раздел `helpful-base`, и
`Qwen/Qwen2.5-0.5B-Instruct` revision
`7ae557604adf67be50417f59c2c2f167def9a775` как общий initial policy и
frozen reference. Prompt — общий префикс пары до последнего `Assistant:`.
Отсортируйте пары по SHA-256 prompt: первые 20 000 идут в DPO, следующие
2 000 в validation, а из остатка возьмите 300 наименьших хэшей для
генерации.

Используйте `transformers==4.48.3`, `trl==0.15.2`, LoRA rank 8,
alpha 16, dropout 0 на `q_proj` и `v_proj`. Prompt и каждый ответ обрезайте
справа до 512 токенов; пустые ответы удаляйте до split. Для каждого
$\beta\in\{0{,}05;0{,}1;0{,}5\}$ выполните ровно 1 000 optimizer steps:
effective batch 64, AdamW $2\cdot10^{-5}$, betas $(0{,}9;0{,}999)$,
weight decay 0, gradient clipping 1, linear warm-up 100 шагов и затем
linear decay; данные циклически перемешиваются seed 2026. Checkpoint —
последний шаг.

На 300 prompts генерируйте по одному ответу policy и reference с temperature
0,7, top-p 0,9, максимум 256 новых токенов, EOS включён. Перед каждым из
двух вызовов создавайте новый `torch.Generator` и задавайте ему
`manual_seed(810000+i)`: policy и reference начинают с одинакового состояния
генератора. KL оцените средним
$\log\pi(y_t\mid y_{<t})-\log\pi_{\rm ref}(y_t\mid y_{<t})$ по токенам
policy-ответов. Distinct-2 — доля различных token-биграмм во всех ответах;
self-BLEU-4 — средний SacreBLEU каждого ответа против остальных. Три
проверяющих вслепую выбирают policy, reference или ничью; win rate считает
победу как 1, ничью как 0,5 и усредняет все 900 оценок. Pair accuracy также
посчитайте на validation-парах, где длины отличаются не более 5% от их
среднего.

До обучения опубликуйте текст и SHA-256 пятидесяти prompts, где корректный
ответ должен признать нехватку данных. Выберите $\beta$ по рубрике:
30 баллов получает максимум validation accuracy; 20 — KL не выше 0,10;
15 — медианная длина в пределах 10% reference; 15 — distinct-2 не ниже
95% reference; 20 — blinded win rate не ниже 55%. Ничью разрешайте меньшим
$\beta$. Опубликуйте split manifest, список prompts, seeds и все критерии.
:::
