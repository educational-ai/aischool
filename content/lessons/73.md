*Рекуррентная сеть читает последовательность по одному элементу и переносит вперёд скрытое состояние. Одни и те же веса работают на каждом шаге, поэтому модель может принимать строки разной длины. Память при этом не дана бесплатно: её надо сохранить сквозь многократное преобразование.*

## Температура без контекста

Датчик сообщает $18{,}1^\circ$, затем $18{,}4^\circ$, затем $18{,}9^\circ$. Последнее число само по себе не говорит, быстро ли нагревается помещение. Нужна история. Можно подать модели окно последних $k$ измерений, но фиксированное окно заранее ограничивает память и дублирует параметры для разных позиций.

Простая recurrent neural network обновляет состояние:

$$
h_t=\tanh(W_xx_t+W_hh_{t-1}+b),
\qquad
\widehat y_t=W_yh_t+c.
$$

$h_t$ — сжатое резюме префикса $x_1,\ldots,x_t$. Матрицы $W_x,W_h$ одинаковы для всех $t$. Это weight sharing: правило обработки не зависит от длины.

Если $h_t$ имеет размер 64, сеть переносит ровно 64 числа, независимо от того, прочитала 10 шагов или 10 000. Ограничение делает модель вычислимой и одновременно заставляет забывать.

:::sidenote{title="Контроль: где хранится порядок?" label="Вопрос"}
Веса на каждом шаге одинаковы. Почему последовательности `АБ` и `БА` всё же могут дать разные $h_2$? Выпишите вложенные формулы.
:::

:::figure{src="/figures/lessons/73/figure-1.svg" id="fig-73-1" title="Одна ячейка, развёрнутая во времени" alt="Рекуррентная ячейка и её развёртка на пять временных шагов"}
Слева RNN изображена циклом, справа тот же расчёт развёрнут в пять копий. Одинаковый цвет матриц показывает общие параметры. Толщина горизонтальной стрелки условно показывает, какая часть импульса первого входа остаётся в $h_t$.
:::

## Линейная память без нелинейности

Чтобы увидеть механику, возьмём одномерную линейную систему:

$$
h_t=wx_t+uh_{t-1},\qquad h_0=0.
$$

Разворачивая рекурсию, получаем

$$
h_t=w(x_t+ux_{t-1}+u^2x_{t-2}+\cdots+u^{t-1}x_1).
$$

При $|u|<1$ прошлое затухает геометрически. Характерное время памяти порядка $1/(1-|u|)$. При $|u|>1$ старые сигналы растут, и состояние становится неустойчивым. При $u=1$ память не затухает, но любая постоянная составляющая накапливается без границы.

Нелинейность $\tanh$ ограничивает значения, однако не отменяет многократного умножения производных. Этот скалярный пример — не игрушка, а рентген сложной сети.

:::definition{title="Скрытое состояние"}
Скрытое состояние $h_t$ — детерминированная функция предыдущего состояния и текущего входа, предназначенная для переноса информации о префиксе последовательности. Оно не обязано иметь прямую предметную интерпретацию.
:::

## Обучение через развёртку

Для последовательности loss складывается по шагам:

$$
\mathcal L=\sum_{t=1}^T\ell(\widehat y_t,y_t).
$$

Backpropagation through time разворачивает вычислительный граф и проводит градиент назад. Вклад от далёкого шага содержит произведение Якобианов:

$$
\frac{\partial h_T}{\partial h_t}
=\prod_{k=t+1}^{T}
\operatorname{diag}(1-h_k^2)W_h.
$$

Если нормы множителей меньше единицы, градиент исчезает; если больше — взрывается. Gradient clipping ограничивает норму:

$$
g\leftarrow g\min\left(1,\frac{c}{\|g\|}\right).
$$

Он спасает от огромного шага оптимизатора, но не возвращает исчезнувший дальний сигнал.

:::sidenote{title="Контроль: память есть, градиента нет" label="Вопрос"}
Состояние может численно зависеть от далёкого входа, но loss не умеет обучить эту зависимость из-за малого градиента. Почему эти два утверждения не противоречат друг другу?
:::

:::figure{src="/figures/lessons/73/figure-2.svg" id="fig-73-2" title="Произведение производных задаёт дальность обучения" alt="Логарифм нормы градиента по временному лагу для трёх спектральных радиусов"}
По горизонтали лаг $T-t$, по вертикали $\log_{10}\|\partial h_T/\partial h_t\|$. Кривые для эффективных множителей $0{,}8$, $1{,}0$, $1{,}15$ показывают исчезновение, сохранение и взрыв. Горизонтальная полоса отмечает диапазон численной различимости.
:::

## Лаборатория импульса

:::widget{name="g11-generative" lesson="73" title="Импульс, скрытое состояние и время забывания"}
:::

Подайте единичный импульс $x_1=1$, остальные входы обнулите. Меняйте рекуррентный множитель и измеряйте время, за которое $|h_t|$ уменьшится вдвое. Сопоставьте опыт с формулой $u^{t-1}$.

Затем включите нелинейность и большой импульс. $\tanh$ насыщается: разные большие значения превращаются почти в единицу. Сеть получает устойчивость по амплитуде, но теряет различимость и производную.

## Какая задача действительно требует памяти

Для проверки дальнего контекста используют synthetic-задачу adding problem. Последовательность содержит случайные числа и два маркера; нужно сложить числа в отмеченных далёких позициях. Локальное окно бесполезно, а точная цель известна.

Другой тест — delayed copy: показать символ, затем длинную паузу, затем потребовать его воспроизвести. Увеличивая задержку, строят кривую дальности памяти. Такие задачи не заменяют реальные данные, но изолируют механизм.

В естественных временных рядах сильный baseline часто проще: последнее значение, скользящее среднее, линейная авторегрессия. Если RNN не выигрывает у них, глубина не оправдана.

:::sidenote{title="Пауза тоже является входом" label="Автор"}
Серия пропусков, нулей и «нет события» — разные вещи. Если ноль может быть настоящим измерением, модели нужна отдельная маска наблюдаемости.
:::

## Truncated BPTT

Для длинной последовательности хранить весь граф дорого. Truncated BPTT обрабатывает отрезки длины $L$: состояние переносится между ними, но градиент через границу обрывается. Модель может использовать информацию в forward pass, хотя обучающий сигнал не проходит дальше $L$ шагов напрямую.

Слишком короткий $L$ лишает сеть причинной связи с далёкой целью. Слишком длинный увеличивает память, время и нестабильность. Длину выбирают по временному масштабу данных, а не круглому числу.

При разбиении нескольких временных рядов нельзя переносить состояние от одного устройства к другому. В batch элементы имеют разную длину; padding маскируют в loss, иначе сеть учится на искусственных нулях.

## Реальные данные: датчики турбины

NASA C-MAPSS содержит синтетически сгенерированные, но физически мотивированные траектории деградации авиационных двигателей: режимы работы, показания сенсоров и момент отказа. Задача — оценить remaining useful life.

Разделять данные нужно по двигателям, а не по строкам времени. Если ранние моменты одного двигателя попадут в train, а поздние — в test, модель узнает индивидуальную траекторию и оценка станет завышенной. Признаки нормируют с учётом режима работы; константные сенсоры удаляют.

RNN получает последовательность до текущего момента и предсказывает остаток. Но target часто обрезают сверху: на ранней стадии точный срок почти неразличим, разумнее предсказывать «не меньше порога».

:::figure{src="/figures/lessons/73/figure-3.svg" id="fig-73-3" title="Деградация видна в траектории, не в одной точке" alt="Два сенсорных ряда двигателей и прогноз remaining useful life"}
Слева две траектории одного сенсора пересекаются: одинаковое текущее значение встречается при разных трендах. Справа показаны истинный RUL, прогноз RNN и baseline по последнему значению. Вертикальная линия отделяет наблюдаемый префикс от будущего отказа.
:::

## RNN, свёртка и внимание

Одномерная свёртка обрабатывает локальные окна параллельно; её receptive field растёт с глубиной и dilation. RNN естественно работает online, обновляя одно состояние. [Attention](/lesson/76) даёт прямой доступ ко всем прошлым позициям, но хранит их и обычно стоит квадратично по длине.

Выбор зависит от задачи. Для потокового датчика с жёсткой памятью рекуррентность остаётся разумной. Для длинного текста, где нужно связать далёкие слова, фиксированное состояние становится узким горлом. [LSTM](/lesson/74) улучшит путь памяти, но не сделает его бесконечным.

:::sidenote{title="Исторический мост" label="История"}
Идея состояния пришла не из нейросетей: дифференциальные уравнения и теория управления давно описывали систему малым набором переменных. RNN учится выбирать такое состояние из данных.
:::

### Мини-исследование: какая история действительно используется

Возьмите обученную модель прогноза нагрузки и проведите temporal occlusion. Для каждого лага $\ell$ замените $x_{t-\ell}$ локальным средним, оставив остальные входы. Измерьте рост ошибки

$$
I(\ell)=
\operatorname{MAE}_{\mathrm{occluded}(\ell)}
-\operatorname{MAE}_{\mathrm{base}}.
$$

Если модель использует суточный и недельный ритм, пики $I(\ell)$ ожидаются около 24 и 168 часов. Но замена одной точки может создавать неестественный скачок. Второй контроль — перемешать целый блок вокруг лага или заменить его фрагментом другого дня с тем же часом.

Сопоставьте occlusion profile с gradient $\|\partial\widehat y_t/\partial x_{t-\ell}\|$. Gradient показывает локальную чувствительность, вмешательство — фактическое изменение output. Их расхождение содержательно: насыщенная RNN может иметь малый gradient, хотя крупное вмешательство меняет состояние.

Добавьте [seasonal baseline](/lesson/41) и тот же occlusion к нему. Если RNN-пик на лаге 168 повторяет явное правило baseline, модель не обязательно открыла новую закономерность. Главный вопрос — уменьшила ли она ошибку в режимах, где недельный повтор не работает: праздники, аномальная погода, пропуски.

### Мини-исследование: скрытый state как ограниченный канал

Обучите одинаковые RNN с $d_h\in\{4,16,64,256\}$ на delayed-copy с несколькими одновременно запоминаемыми символами. Для каждого размера измерьте accuracy против числа символов и задержки, а также время и параметры. Малый state исчерпывает ёмкость, большой труднее оптимизируется. Получится empirical frontier, а не лозунг «больше памяти лучше».

С помощью linear probe попробуйте декодировать каждый сохранённый символ из $h_t$ на разных шагах. Probe не доказывает использование информации, но показывает, когда она становится недоступна простому чтению. Сравните с фактическим output модели.

Добавьте shuffle-control: перемешайте временной порядок внутри окна, сохранив набор значений. Если качество почти не изменилось, модель использует агрегат, а не последовательность. Тогда более простой [табличный baseline](/lesson/49) со средним и трендом может быть достаточен.

В реальном ряду перемешивание разрушает сезонность, поэтому делайте его внутри сопоставимых часов или synthetic-задачи. Контроль должен удалять исследуемую структуру и сохранять остальные, иначе причина падения неясна.

## Память измеряется задачей

RNN — повторяемое правило обновления состояния. Развёртка показывает и экономию параметров, и проблему произведения производных. Простые импульсные тесты объясняют память лучше случайной метрики на одном датасете. А на реальных рядах честное разделение по объектам важнее архитектурного названия.

## Задачи

:::problem{points="3"}
Для скалярной линейной RNN
$h_t=2x_t+0{,}5h_{t-1}$ задано $h_0=0$ и
$(x_1,\ldots,x_5)=(1,0,0,0,0)$. Вычислите $h_1,\ldots,h_5$, затем выведите
формулу $h_t=2(0{,}5)^{t-1}$ для этого импульса и найдите первый целый $t$,
когда $|h_t|<0{,}1$. Случайности и единиц измерения нет; ответ принимается,
если рекуррентный и явный расчёты дают один порог.
:::

:::problem{points="4"}
Пусть $h_t=uh_{t-1}$, $h_0=1$, $T=50$, а
$\mathcal L=\tfrac12h_T^2$. Выведите точную формулу
$\partial\mathcal L/\partial h_0$ через $u,T,h_0$ и вычислите её для
$u\in\{0{,}9;1;1{,}1\}$. Случайности и единиц измерения нет; зачёт требует
научную запись трёх чисел с тремя значащими цифрами и объяснение затухания
или роста через произведение 50 Якобианов.
:::

:::problem{points="5"}
Для каждой задержки $D\in\{10,30,100,300\}$ создайте отдельный delayed-copy
набор: $x_0$ равномерно принимает один из восьми символов, следующие $D$
входов равны девятому символу `blank`, а по последнему скрытому состоянию
нужно классифицировать $x_0$. Train/validation/test содержат ровно
80 000/10 000/10 000 последовательностей и точно поровну восемь классов;
порядок перемешивают PCG64 seeds $7330+D$, $7331+D$, $7332+D$.
Модель для всех $D$ одна: one-hot вход размера 9, однослойная Elman RNN
$h_t=\tanh(W_xx_t+W_hh_{t-1}+b)$ с $h_0=0$, ширина 32, линейный выход
32→8. Инициализация Xavier, PyTorch seed $7333+D$; Adam $10^{-3}$,
batch 256, cross-entropy, максимум 30 эпох, early stopping patience 4 по
validation accuracy, gradient clipping 1. Зачёт требует test accuracy,
случайный baseline $1/8$ и медианы
$\lVert\partial\mathcal L/\partial h_t\rVert_2$ для лагов
1, 10, 30, 100 и 300, где лаг существует.
:::

:::problem{points="6"}
Используйте Open Power System Data, пакет `time_series` версии `2020-10-06`,
файл `time_series_60min_singleindex.csv`, столбец
`DE_load_actual_entsoe_transparency` для Германии в МВт и часы UTC от
2015-01-01 до 2019-12-31. Train равен 2015–2017, validation 2018, test 2019;
внутри каждой части линейно интерполируйте все внутренние серии не более чем
из шести последовательных пропусков, не перенося значения через границы
частей. Начальные, конечные и более длинные серии оставьте как NaN, после
чего удалите каждое окно, где хотя бы один из 168 входов или цель остаётся
NaN. Из последних 168 нагрузок предскажите следующий час: persistence
$\widehat y_t=y_{t-1}$, ridge-регрессия с
$\lambda=1$, однослойная tanh-RNN ширины 32 и линейный выход. Нормировка
средним и стандартным отклонением train; RNN: $h_0=0$, Xavier weights,
нулевые biases, Adam $10^{-3}$, batch 128, truncated BPTT 168, 30 эпох,
early stopping 4, seeds
`7341`–`7345`. Пиковый test-час имеет истинную нагрузку выше 90-го процентиля
train. Зачёт требует число параметров, MAE в МВт для всех и пиковых часов и
95%-интервал по пяти RNN seed.
:::

:::problem{points="7"}
Возьмите NASA Prognostics Data Repository, `Turbofan Engine Degradation
Simulation Data Set` 2008, архив `CMAPSSData.zip`, только subset `FD001`.
Train-двигатели с ID 1–80 служат train, 81–100 validation; официальный
`test_FD001.txt` со 100 двигателями и `RUL_FD001.txt` служит test. Для каждого
train- и validation-цикла $t$ цель равна
$y=\min(125,t_{\rm last}-t)$, где $t_{\rm last}$ — последний цикл того же
двигателя. Строка $r_i$ файла `RUL_FD001.txt` задаёт число циклов после
последнего наблюдаемого цикла test-двигателя с ID $i$; поэтому для каждого
его test-цикла используйте
$y=\min(125,r_i+t_{\rm last}-t)$. Диапазоны ошибок равны 0–30, 31–80 и
81–125 циклов. Удалите постоянные sensors
1,5,6,10,16,18,19, остальные sensors и три setting стандартизируйте по train.
Префикс обрежьте до последних 50 циклов и дополните слева маской. Сравните:
MLP 17→64→1 по последнему вектору; 1D-CNN с channels 32, kernel 5,
global-average pooling; GRU ширины 32 по последовательности. Все модели
начинаются с Xavier weights, нулевых biases и нулевого hidden state,
оптимизируют MSE Adam $10^{-3}$, batch 128, до 60 эпох, patience 6,
PyTorch seeds `7351`–`7355`. Зачёт требует test RMSE в циклах, MAE трёх
диапазонов, среднее и стандартное отклонение по seed и два test-префикса с
близкими последними векторами, но различным RUL.
:::
