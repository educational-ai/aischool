*StyleGAN разделяет случайный код и управление масштабами генератора: ранние уровни задают геометрию, поздние уточняют текстуру. Та же чувствительность нейросетей, которая делает редактирование плавным, напоминает о другой стороне — малые направленные возмущения могут резко менять решение.*

## Почему исходный latent неудобен

В обычном [GAN](/lesson/82) шум $z\sim\mathcal N(0,I)$ подаётся прямо в generator. Координаты $z$ не обязаны соответствовать осмысленным изменениям. Путь между двумя кодами может менять позу, фон и идентичность одновременно.

StyleGAN вводит mapping network:

$$
w=f(z),
$$

а affine-преобразования $A_\ell(w)$ управляют слоями synthesis network. Mapping может «развернуть» распределение и сделать направления в $w$ более удобными для генерации.

Generator начинает с обучаемой константы, а не с пространственной карты, полученной из $z$. На каждом масштабе style модулирует каналы свёртки; случайный noise добавляет мелкие стохастические детали.

:::sidenote{title="Контроль: где находится картинка?" label="Вопрос"}
$w$ — не уменьшенное изображение и не имеет пиксельных координат. Что тогда означает «двигаться по направлению улыбки»? Это изменение параметров генерации, найденное эмпирически.
:::

:::figure{src="/figures/lessons/83/figure-1.svg" id="fig-83-1" title="Код входит в генератор на нескольких масштабах" alt="Архитектурная схема StyleGAN mapping z to w and style modulation at resolutions"}
Слева $z$ проходит mapping network и даёт $w$. Справа synthesis растёт от $4\times4$ до $1024\times1024$; одинаковый $w$ через разные affine-блоки управляет слоями. Крупные и мелкие признаки подписаны у соответствующих разрешений.
:::

## Модуляция и демодуляция

Пусть style даёт коэффициенты $s_i$ для входных каналов свёртки. Веса модулируются:

$$
w'_{ijk}=s_iw_{ijk}.
$$

Затем demodulation нормирует выходной канал:

$$
w''_{ijk}=
\frac{w'_{ijk}}
{\sqrt{\sum_{i,k}(w'_{ijk})^2+\varepsilon}}.
$$

Так style меняет относительный вклад каналов, а масштаб активаций остаётся управляемым. Это не «вставить цвет глаз» готовым параметром; признаки возникают в совместно обученном базисе.

Path length regularization поощряет близкий масштаб изменения изображения при одинаковом шаге в latent:

$$
\mathbb E_{w,y}
\left(
\|J_w^\top y\|_2-a
\right)^2.
$$

Она делает геометрию более ровной, но не гарантирует независимых семантических осей.

## Style mixing как эксперимент

Возьмём коды $w_A,w_B$. До некоторого слоя используем $w_A$, после — $w_B$. Ранние слои работают на малом пространственном разрешении и меняют глобальную компоновку; поздние — локальные детали.

Матрица mixing experiment должна показывать не лучшие четыре примера, а фиксированную сетку источников и границу слоёв. Если идентичность меняется поздно, заявленное разделение масштабов неидеально.

:::sidenote{title="Контроль: слой и признак не синонимы" label="Вопрос"}
Ранний слой имеет низкое разрешение. Может ли он влиять на цвет? Да: глобальный цвет относится к крупному масштабу изображения. «Ранний = поза, поздний = волосы» лишь тенденция.
:::

:::figure{src="/figures/lessons/83/figure-2.svg" id="fig-83-2" title="Матрица style mixing отделяет масштабы" alt="Сетка генераций с coarse style по строкам fine style по столбцам"}
Строки задают $w_A$ на слоях $4$–$32$, столбцы — $w_B$ на $64$–$1024$. Поля фигуры перечисляют наблюдаемые изменения: ракурс и форма следуют строкам, текстура и фон чаще следуют столбцам. Исключения отмечены, а не скрыты.
:::

## Лаборатория масштаба

:::widget{name="g11-generative" lesson="83" title="Крупный стиль, мелкая текстура и adversarial-сдвиг"}
:::

Передвигайте границу style mixing. Для каждого уровня фиксируйте, какие измеримые признаки изменились: положение landmarks, средний цвет фона, частотный спектр текстуры. Не полагайтесь только на словесное впечатление.

Во второй части добавьте малое направленное возмущение к изображению classifier-а. Сравните визуальную величину и сдвиг logits. Генеративное редактирование и adversarial perturbation различаются целью, но оба изучают чувствительность функции в многомерном пространстве.

## Инверсия: найти код существующего изображения

Для реального $x$ ищут $w$:

$$
w^*=\arg\min_w
\left[
\lambda_{\mathrm{pix}}\|G(w)-x\|_2^2
+\lambda_{\mathrm{perc}}
\|\phi(G(w))-\phi(x)\|_2^2
\right].
$$

Pixel loss хранит точные цвета, perceptual loss — признаки сети $\phi$. Если объект лежит вне learned manifold, точной реконструкции нет. Расширенное $W^+$ разрешает отдельный style каждому слою и реконструирует лучше, но редактирование становится менее устойчивым.

После инверсии можно двигать $w^*+\alpha d$, где $d$ — найденное направление признака. Проверяют не только целевое изменение, но и сохранение identity и фона.

:::sidenote{title="Реконструкция не доказывает происхождение" label="Проверка"}
Если generator хорошо восстанавливает фото, это не значит, что фото было им создано. Мощная модель умеет приближать и реальные изображения.
:::

## Состязательное возмущение

Classifier $f_\theta(x)$ можно атаковать в ограничении $\|\delta\|_\infty\le\varepsilon$. Fast Gradient Sign Method:

$$
x_{\mathrm{adv}}=
x+\varepsilon\operatorname{sign}
\nabla_x\mathcal L(f_\theta(x),y).
$$

Каждый пиксель меняется мало, но миллионы согласованных изменений суммируются в признаках. Projected Gradient Descent повторяет маленькие шаги и проецирует обратно:

$$
x^{k+1}=
\Pi_{B_\varepsilon(x)}
\left(x^k+\alpha\operatorname{sign}
\nabla_x\mathcal L(f_\theta(x^k),y)\right).
$$

Норма задаёт threat model. $\ell_\infty$ ограничивает каждый пиксель, но не гарантирует перцептивную незаметность. Поворот, печать или изменение освещения требуют других множеств преобразований.

:::figure{src="/figures/lessons/83/figure-3.svg" id="fig-83-3" title="Малое по норме, большое по решению" alt="Исходное изображение adversarial perturbation усиленная в 10 раз и график logits"}
Показаны исходник, $\delta$ с десятикратным усилением для видимости и $x+\delta$. Под ними logits правильного и ошибочного классов по шагам PGD; вертикальная линия отмечает пересечение decision boundary.
:::

## Защита и честная атака

Adversarial training минимизирует worst-case loss:

$$
\min_\theta
\mathbb E_{(x,y)}
\max_{\|\delta\|\le\varepsilon}
\mathcal L(f_\theta(x+\delta),y).
$$

Внутренний максимум приближает PGD. Robust accuracy обычно ниже clean accuracy: устойчивость имеет цену. Слабая атака может создать ложное чувство защиты; нужна разная инициализация, достаточно шагов и проверка градиентов.

Gradient masking делает gradient бесполезным, но не модель устойчивой. Black-box transfer или gradient-free атака иногда обходят маскировку.

Связь с [валидацией моделей](/lesson/44) принципиальна: threat model, бюджет атаки и adaptive adversary входят в условие метрики.

Сравнивать защиты можно лишь при одинаковом бюджете атаки. Число шагов, restart, размер шага и доступ к градиенту запишите рядом с robust accuracy, иначе одна цифра не воспроизводит эксперимент.

:::sidenote{title="Атака знает защиту?" label="Автор"}
Если противник в реальности видит модель или может посылать запросы, оценивать только случайный шум нечестно. Сила теста должна учитывать предполагаемые возможности атакующего.
:::

## FFHQ и ответственность данных

FFHQ содержит 70 000 изображений лиц, собранных из Flickr с лицензиями. Набор разнообразнее ранних celebrity datasets, но не представляет случайную выборку мира. Генерация лиц создаёт риски для идентичности и согласия и открывает возможность злоупотребления.

Аудит StyleGAN включает:

- nearest neighbors и memorization;
- качество по возрастным и визуальным группам;
- failure cases аксессуаров и фона;
- provenance output;
- ограничения распространения весов;
- проверку редактирования на сохранение identity.

Техническая редактируемость не равна этической допустимости применения.

### Мини-исследование: редактирование без скрытой подмены

Выберите measurable attribute, например угол поворота головы, и direction $d$ в latent. Для 200 исходных кодов примените $\alpha\in[-3,3]$. Target effect измерьте pose estimator-ом, collateral effects — identity similarity, фон, яркость и другие атрибуты. Покажите среднюю кривую вместе с worst 10%.

Если pose растёт монотонно, а identity резко меняется после $|\alpha|>1{,}5$, у направления есть рабочий диапазон. Интерфейс редактирования должен ограничивать slider этим диапазоном и показывать, что край — экстраполяция.

Проведите cycle test: примените $+\alpha d$, затем $-\alpha d$. В идеальной линейной геометрии вернётся исходник. Reconstruction error обнаруживает нелинейность и clipping. Сравните $W$ и $W^+$: второй лучше реконструирует, но может хуже выдерживать цикл.

Такой протокол соединяет [инверсию VAE](/lesson/85) и StyleGAN: latent edit оценивается как преобразование с target и побочными эффектами, а не как галерея удачных лиц.

### Мини-исследование: perceptual и нормативная малость

Для adversarial perturbations вычислите одновременно $\ell_\infty$, $\ell_2$, LPIPS и результат печать–фото или JPEG. Малое значение одной нормы не означает незаметность в другой. Покажите scatter attack success против каждой меры.

Попросите людей сравнить пары вслепую, не сообщая, где атака. Их detection rate даёт дополнительный perceptual контроль, но не делает возмущение допустимым: в медицинском изображении изменение нескольких пикселей может быть предметно значимо.

Наконец, сравните random noise той же нормы. Если оно почти не меняет prediction, эффект объясняет направленность gradient. Это связывает атаку с [оптимизацией по градиенту](/lesson/53), а не с общей хрупкостью к любому шуму.

Добавьте targeted attack: требуется не просто любой неверный класс, а заданный. Сравните необходимую норму и число шагов с untargeted режимом. Targeted задача обычно труднее и лучше проверяет, управляет ли атака решением.

Проверьте physical transform: случайные crop, JPEG и небольшую перспективу на каждом PGD-шаге через expectation over transformations. Если perturbation сохраняет эффект, threat сильнее. Но по-прежнему не называйте его «незаметным», пока это не проверено людьми и предметными ограничениями.

Для защиты сравните white-box attack на защищённую модель и transfer от обычной. Если white-box неожиданно слабее чёрного ящика, вероятно gradient masking. Robust evaluation должна пытаться опровергнуть устойчивость.

Не смешивайте robustness и calibration. Модель может сохранить правильный класс под атакой, но выдавать чрезмерную уверенность, либо менять класс и честно снижать confidence. Стройте robust accuracy, negative log-likelihood и abstention curve.

При randomized defense attack должен усреднять gradient по случайности. Один forward создаёт шумную оценку и завышает защиту. Все параметры атаки, число restart и критерий успеха входят в воспроизводимое условие.

## Геометрия должна выдержать вмешательство

StyleGAN делает latent управление многоуровневым и исследуемым через mixing и inversion. Но интерпретируемость направлений неполна. Adversarial examples показывают, насколько локальная геометрия сети может расходиться с человеческим восприятием. В обоих случаях надёжный вывод требует измерения, а не одной впечатляющей картинки.

## Задачи

:::problem{points="3"}
Два входных канала свёртки имеют style $(2,0{,}5)$, а веса одного выходного канала до модуляции равны $(1,3)$. Вычислите модулированные веса и их demodulation при $\varepsilon=0$. Сравните норму до и после.
:::

:::problem{points="4"}
Для линейного classifier loss gradient по четырём пикселям равен $(0{,}2,-3,0,1{,}5)$. Постройте FGSM-возмущение при $\varepsilon=0{,}01$, найдите его $\ell_\infty$- и $\ell_2$-нормы и скалярное произведение с gradient.
:::

:::problem{points="5"}
Сконструируйте двумерный generator $G(w_1,w_2)$, где один latent-направление меняет одновременно два наблюдаемых признака. Покажите Jacobian и найдите линейную замену координат, которая локально разделяет признаки, если Jacobian невырожден. Объясните, почему глобальное разделение из локального не следует.
:::

:::problem{points="6"}
Используйте StyleGAN2-ADA revision
`d72cc7d041b42ec8e806021a205ed9349f87c6a4` и checkpoint
`https://nvlabs-fi-cdn.nvidia.com/stylegan2-ada-pytorch/pretrained/ffhq.pkl`,
`noise_mode=const`, truncation $\psi=0{,}7$. Для пар seeds
$(0,1),(2,3),\ldots,(18,19)$ получите два массива $w^+$ из 18 style-векторов.
Границам $4,8,16,32,64,128,256,512$ сопоставьте cutoffs
$c=2,4,6,8,10,12,14,16$ в том же порядке: в смесь входят
$w^+_{0:c}$ первого изображения и $w^+_{c:18}$ второго. Индексация нулевая,
cutoff не входит в первую часть; порядок Conv и ToRGB остаётся штатным для
checkpoint. Сохраните оба исходника и восемь смесей без отбора.

Для $Y=0{,}2126R+0{,}7152G+0{,}0722B$ измерьте ровно три скалярных
признака: горизонтальный центр яркости
$c_x=\sum xY_{xy}/\sum Y_{xy}$, линейно приведённый к $[-1,1]$; среднюю
luminance центрального квадрата $256\times256$ после Gaussian blur
$\sigma=16$; энергию
$E=(HW)^{-1}\sum_{x,y}(\Delta Y_{xy})^2$, где $\Delta$ — 4-соседний
Laplacian с отражающей границей. Для каждой границы и признака paired effect
равен среднему из десяти изменений относительно первого изображения,
делённому на их sample standard deviation; при нулевой дисперсии запишите
0, если среднее тоже 0, иначе signed infinity. Покажите сетку $10\times10$
для каждой пары и три графика effect size по слоям.
:::

:::problem{points="7"}
Используйте CIFAR-10 из `torchvision==0.20.1` и checkpoints
`cifar10_resnet20`, `cifar10_vgg16_bn` из
`chenyaofo/pytorch-cifar-models` revision
`786c16252c0fc58ee9adac063f8337cc4a7a497a`. Выберите 2 000 test-индексов
с наименьшим SHA-256 строки `index|2026`. Атакуйте RGB в $[0,1]$ с clipping;
в сети применяйте mean $(0{,}4914,0{,}4822,0{,}4465)$ и std
$(0{,}2470,0{,}2435,0{,}2616)$. Сравните clean, FGSM и untargeted
$\ell_\infty$-PGD при $\varepsilon\in\{1,2,4,8\}/255$. PGD делает 20 шагов
$\varepsilon/4$; start $r$ объекта $i$ равномерен в кубе из PCG64 seed
`8307000+100*i+r`, $r=0,\ldots,9$. Устойчивость означает, что все десять
стартов сохранили истинный класс.

Обучите ResNet-20 с нуля adversarial training: официальный train split,
random crop с padding 4 и horizontal flip, batch 128, SGD momentum 0,9,
weight decay $5\cdot10^{-4}$, 100 эпох, cosine learning rate от 0,1 до 0,
seed 2026. Каждый batch атакуйте PGD-7 при $8/255$, step $2/255$, один
uniform start с seed `8310000+global_batch`. Для каждого
$\varepsilon\in\{1,2,4,8\}/255$ создайте на VGG16-BN transfer-примеры
PGD-20 с десятью starts, заданными формулой выше, и оцените обе ResNet-20.
Сообщите clean/robust accuracy и покажите первые 20 по хэшу clean-верных
примеров, сломанных PGD: истинный класс, оба ответа и возмущение, усиленное
ровно в восемь раз для показа.
:::
